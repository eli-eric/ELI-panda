name: Compose docker grafana

on:
  workflow_dispatch:

defaults:
  run:
    working-directory: tools/grafana

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Build the Docker compose
        run: docker-compose up -d --build
