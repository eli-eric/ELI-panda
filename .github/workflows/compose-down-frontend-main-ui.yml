name: Compose down frontend

on:
  workflow_dispatch:

jobs:
  compose-down:
    runs-on: czechia-server

    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker compose
        run: docker compose down
