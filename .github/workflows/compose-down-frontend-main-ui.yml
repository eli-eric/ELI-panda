name: Compose down frontend MAIN UI APP

on:
  workflow_dispatch:

defaults:
  run:
    working-directory: frontend/ui-main-app

jobs:
  compose-down:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Build the Docker compose
        run: docker-compose down
