name: Compose up backend API GATEWAY

on:
  workflow_dispatch:

defaults:
  run:
    working-directory: backend/api-gateway-go

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Build the Docker compose
        run: docker-compose up -d --build
