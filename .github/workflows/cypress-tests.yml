name: Cypress Tests

on: [push]

defaults:
  run:
    working-directory: frontend/ui-main-app

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker compose
        run: docker-compose -f docker-compose-cypress.yaml up -d --build
