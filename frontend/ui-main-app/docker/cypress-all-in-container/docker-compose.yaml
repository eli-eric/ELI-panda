version: '3.9'

services:
  cypress-tests-with-app-inside-container:
    container_name: cypress-tests-with-app-inside-container
    restart: "no"
    build:
      context: ../../
      dockerfile: Dockerfile.cypress-all-in-container
      labels:
        - cypress-tests-with-app-inside-container
