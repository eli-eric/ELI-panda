# Ralph Loop — Technical Documentation Authoring

You are authoring technical documentation for ELI PANDA across GitHub issues
#1098–#1112 (repo: `eli-eric/ELI-panda`, label: `docs:technical`). One issue per
iteration. Work persists across iterations on branch `feat/tech-docs` with a
single rolling PR. Stop when all 15 issues are closed and emit
`<promise>TECH_DOCS_COMPLETE</promise>`.

---

## ITERATION 0 (only if `docs/.tech-docs-tracker.md` does not exist)

1. Map `docs/` exhaustively. Read every file under:
   - `docs/Home.md`
   - `docs/technical/README.md` (planned-sections list — your TOC)
   - `docs/user-guide/README.md`
   - `docs/user-guide/_template/README.template.md`
   - `docs/user-guide/_template/workflow.template.md`
   - `docs/user-guide/systemHierarchy/README.md` (canonical tone reference)
   - `docs/user-guide/systemHierarchy/workflows/copying-systems.md`
   - `docs/implementation-plans/*.md` (background, not output format)
2. Internalize: voice, persona blocks, 🔮 callouts for planned work, 🔧
   "data model reference" footer, screenshot/video placeholders style.
3. Run `gh issue list -R eli-eric/ELI-panda -l docs:technical -s open --limit 30`
   → write `docs/.tech-docs-tracker.md` listing #1098–#1112 with status
   `[ ] pending` and a one-line scope each. Order:

   `#1108 → #1109 → #1110 → #1111 → #1112 → #1098 → #1099 → #1100 → #1101 → #1102 → #1103 → #1104 → #1105 → #1106 → #1107`

4. Verify branch `feat/tech-docs` exists and is checked out (create from
   `dev` if missing, otherwise rebase on `dev`).
5. Commit the tracker. Continue to next iteration — do NOT promise yet.

---

## EVERY OTHER ITERATION — pick ONE pending issue from the tracker

### Pre-flight
- Re-read `docs/.tech-docs-tracker.md` and pick the first `[ ] pending`.
- Re-read the chosen issue body: `gh issue view <N> -R eli-eric/ELI-panda`.
- Re-read `docs/user-guide/_template/README.template.md` and the systemHierarchy
  README each iteration — DO NOT drift from the established structure.

### STEP 1 — PLAN (write briefly to scratch, do not commit)
- Output path: `docs/technical/<kebab-topic>.md` (or directory with subpages
  if the issue's scope is large — e.g. systems family).
- List sections you will produce (mirror existing tone: Overview → Stack /
  Data model → Schema & authorization → Module map → Key flows → Integration
  points → Deprecated / legacy → Maintenance recommendations → 🔮 Planned →
  Open questions).
- List the Mermaid diagrams you will draw (see STEP 4 — at least one is
  mandatory; pick the type that fits the topic).

### STEP 2 — MAP THE MODULE (source of truth = code)
- List every file in the module folder(s) the issue cites (use `find`
  `src/modules/<folder>` and group by `.cont.tsx` / `.comp.tsx` / hooks /
  queries / mutations / store / types / utils / `__tests__`).
- Schema: grep `src/server/apollo/schema.graphql` for the entities and
  relationships named in the issue. Quote `@authorization` blocks verbatim.
- Generated types: cross-reference `src/types/gql/`.
- Pages/routes: `src/pages/**` matching the module.
- Codegen targets, fetcher hooks (`queryFetcher` / `queryMutate`), Zustand
  stores, React Hook Form + Zod schemas — note their roles.
- Identify integration points to OTHER modules (cross-references).

### STEP 3 — DEPRECATION & MAINTENANCE AUDIT (mandatory section in every doc)
- Grep the module for: `@deprecated`, `// DEPRECATED`, `TODO`, `FIXME`,
  `HACK`, `XXX`, `legacy`, `OLD_`, `unused`, feature flags that are off.
- Identify files with low/no recent activity (`git log --since=1.year.ago --name-only -- src/modules/<folder>` — flag files absent from output).
- Flag duplicate/competing patterns (e.g. two stores doing similar things,
  parallel old/new query hooks, dead exports).
- Note components still using HeadlessUI vs migrated to shadcn/ui, raw
  fetch vs `queryFetcher`, class components, untyped `any`.
- Produce two subsections in the doc:

  ```markdown
  ## Deprecated / legacy
  - `<file>:<line>` — `<symbol>` — what it is, why it's deprecated,
    replacement (if known), removal blocker.

  ## Maintenance recommendations
  - Prioritized bullets: refactor X to Y, delete dead code Z, migrate to
    shadcn, add tests for hot path, etc. Each with rationale.
  ```

### STEP 4 — WRITE THE DOC (`docs/technical/<topic>.md`)
- Match the systemHierarchy README cadence: short paragraphs, code-fenced
  file paths, tables for option matrices.
- **Mermaid diagrams are mandatory — at least one per page.** Pick the
  fitting type(s):
  - `erDiagram` for data-model / schema relationships
  - `flowchart` for control flow, codegen pipeline, request lifecycle
  - `sequenceDiagram` for multi-actor flows (UI → fetcher → resolver → DB)
  - `classDiagram` for module-internal type hierarchies
  - `stateDiagram-v2` for entity lifecycles (e.g. order/item states)

  Verify each diagram renders by checking syntax against the Mermaid spec
  (no trailing commas, quoted labels with special chars, valid arrow syntax).
- Cite repo paths with backticks (`` `src/modules/...` ``). Quote schema snippets.
- Mark planned/future work with 🔮 callouts. Engineering-precise language.
- Cross-link relevant user-guide pages where one exists.
- Include the standard 🔧 "Data model reference" footer pointing to
  `src/server/apollo/schema.graphql` for authoritative shapes.
- End with an "Open questions" section if the audit surfaced ambiguities.

### STEP 5 — UPDATE INDEXES
- Flip the matching bullet in `docs/technical/README.md` from planned to a
  real link.
- Append the page to `docs/Home.md` Technical Documentation section if it
  is a top-level topic.
- Mark `[x] done` in `docs/.tech-docs-tracker.md` with the doc path.

### STEP 6 — COMMIT & PR (single rolling PR)
- Stage only `docs/` changes (no source edits, ever).
- Commit on `feat/tech-docs`: `docs(technical): <topic> (#<N>)`.
- First time: `gh pr create -B dev -H feat/tech-docs --title "docs(technical): batch #1098–#1112" --body "Rolling PR for technical docs. Closes #<N>."`
- Subsequent iterations: push, then `gh pr edit <PR> --body "<previous body>\nCloses #<N>"` so every covered issue is referenced.
- Do NOT add Co-Authored-By.

### STEP 7 — DECIDE
- Count remaining `[ ] pending` in tracker.
- If > 0: end iteration WITHOUT promise. Ralph will respawn you.
- If == 0: verify the PR lists all 15 issues, then output
  `<promise>TECH_DOCS_COMPLETE</promise>`.

---

## HARD RULES

- One issue per iteration. Never batch unless an issue trivially fits another.
- Source of truth = code & schema. No speculation. Verify with `git grep`.
- Stick to templates. Re-read `_template/` and the systemHierarchy README each
  iteration to prevent drift.
- At least one valid Mermaid diagram per page. No exceptions.
- Only edit `docs/`. Never touch `src/`.
- Never run `yarn dev`. Never skip git hooks.
- If an issue is ambiguous, write what the code does today and add an
  "Open questions" section — do not invent intent.
- If a planned-section bullet in `docs/technical/README.md` does not match
  any issue, leave it.
