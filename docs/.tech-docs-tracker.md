# Tech docs tracker

Rolling state for the technical-docs Ralph loop. One issue per iteration in the order below. Flip `[ ]` → `[x]` once the doc is committed and the issue is referenced in the rolling PR.

**Repo:** `eli-eric/ELI-panda` · **Label:** `docs:technical` · **Branch:** `feat/tech-docs`

## Cross-cutting first (foundation)

- [x] **#1108** — Cross-cutting — App architecture · foundation doc: Next.js 14 + GraphQL/Neo4j + TanStack Query + RHF/Zod + module layout. (`docs/technical/app-architecture.md`)
- [x] **#1109** — Cross-cutting — Authentication · NextAuth + Microsoft Entra ID, JWT/session shape, login flow. (`docs/technical/authentication.md`)
- [x] **#1110** — Cross-cutting — Permissions model · `systems-view` / `systems-edit` / `admin` + per-module roles, `@authorization` directives, planned Phase 1/2 tightening. (`docs/technical/permissions-model.md`)
- [x] **#1111** — Cross-cutting — Deployment & runbook · Azure dev/test/prod envs, `.github/workflows/`, Docker images, release procedure. (`docs/technical/deployment-runbook.md`)
- [ ] **#1112** — Cross-cutting — Local development & conventions · prereqs, `yarn install`, codegen, test conventions, coding style from `CLAUDE.md`.

## Module docs

- [ ] **#1098** — Systems family architecture · `systemHierarchy`, `systems`, `systemItem`, `systemsRelations`, `systemsMoving`, `systems-multi-move`. _Large topic — likely a directory with subpages._
- [ ] **#1099** — Catalogue & Items · `catalogue/`, `catalogueItem/` + `CatalogueCategory*` schema types and property bag model.
- [ ] **#1100** — Orders & Order Items · `orders/`, `orderItem/` + `Order` / `Item` / `ServiceItem` and `hasOrderLine` / `isServicedBy` interfaces.
- [ ] **#1101** — Services · `services/`, `serviceTypeItem/` + `ServiceItem` lifecycle and item-servicing target.
- [ ] **#1102** — Room Cards · `roomCards/`, `roomCard/` + `RoomCard` / `OperationalState` / `HallContactPerson` and status/schedule enums.
- [ ] **#1103** — Codebooks · `codebooks/` — small reference entity types (`SystemImportance`, `Unit`, etc.) and how they back option lists.
- [ ] **#1104** — Control Systems · `control-systems/` — see `docs/implementation-plans/control-systems-module.md` for background.
- [ ] **#1105** — Zones · `zones/` + `Zone`, `HAS_SUBZONE`, `HAS_ZONE` relationships.
- [ ] **#1106** — Publications · `publication/`, `publications/`, `researchers/`, `grants/`.
- [ ] **#1107** — Administration (Users & Roles) · `administration/` + `User` / `Role` / `UserSettings` and JWT type.

## Process reminder

Each iteration: re-read this tracker, pick the first `[ ] pending`, follow the steps in `docs/.ralph-tech-docs-prompt.md`. After committing, flip the box to `[x] done` with the doc path appended in parentheses, e.g.:

```
- [x] **#1108** — Cross-cutting — App architecture · … (`docs/technical/app-architecture.md`)
```
