version: "3"

services:
  minio:
    image: minio/minio
    ports:
      - "9000:9000"
    volumes:
      - ./s3data:/data
    environment:
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-12345678}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-12345678}
    command: server /data

